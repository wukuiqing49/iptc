# CaptionMeta SEO 与 ASO 优化说明

## 1. 结论

CaptionMeta 不应只围绕 `IPTC` 获取用户。产品实际能力是：

**EXIF + IPTC + XMP 元数据编辑、隐私清理、批量预设和照片交付。**

推荐的获客结构是：

**Photo Metadata Editor → EXIF Editor → IPTC Editor → XMP Editor**

其中：

- `Photo Metadata` 和 `EXIF` 用于覆盖更广的用户需求。
- `IPTC` 和 `XMP` 用于体现摄影记者、图片机构和专业工作流的差异化。
- `Android`、本地处理、批量预设和照片交付用于筛选真实目标用户。

这是一项关键词假设，不是已验证的排名结论。正式投入前，必须结合目标国家、语言、搜索时间、Google 搜索结果和 Google Play 竞品数据复核。

## 2. 产品与用户定位

### 核心定位

CaptionMeta 是一款面向 Android 的照片元数据工作工具，用于在本地照片上检查和编辑支持的 EXIF、IPTC、XMP 字段，清理选定的隐私元数据，使用批量预设，并将处理后的照片交付到用户配置的服务器。

### 主要用户

- 摄影师和摄影记者
- 图片机构和编辑团队
- 现场、活动和纪实团队
- 需要批量统一照片说明、关键词、署名或版权信息的用户

### 产品表达边界

可以表达：

- edit photo metadata
- edit EXIF, IPTC, and XMP fields
- clean selected private metadata
- apply batch metadata presets
- prepare and deliver processed photo copies
- Android app

不要表达：

- online photo metadata editor
- CaptionMeta 提供云存储
- 保证隐私、绝对安全或保证安全传输
- 无限存储、免费服务或未确认的价格
- 评分、奖项、用户数量或市场领先地位
- 支持所有照片格式或所有元数据字段

## 3. 关键词策略

### 3.1 关键词层级

| 层级 | 关键词方向 | 作用 | 当前策略 |
| --- | --- | --- | --- |
| 核心入口 | `photo metadata editor`、`photo metadata app` | 覆盖产品类别需求 | 首页重点验证 |
| 高意图入口 | `exif editor`、`photo exif editor`、`remove exif metadata` | 覆盖编辑和隐私清理需求 | 首页、隐私页和元数据页分工覆盖 |
| 专业差异化 | `iptc editor`、`iptc metadata editor`、`xmp editor` | 覆盖专业用户 | 元数据编辑页重点覆盖 |
| 工作流长尾 | `batch iptc metadata`、`photo caption editor`、`sftp photo delivery` | 覆盖真实使用场景 | 批处理页、交付页和博客覆盖 |
| 排除项 | `exif editor online`、`free unlimited storage` | 与产品或已知事实不匹配 | 不创建对应页面，不在文案中承诺 |

### 3.2 搜索量数据的使用规则

当前文档中的 Keyword Planner 区间只能作为初始假设。每条数据应补充以下字段后才能用于资源排序：

- 国家或地区，例如 United States、United Kingdom、India
- 语言和查询月份
- 月搜索量区间及其设置条件
- 竞争度和页面广告密度
- 搜索结果中的产品类型：Android App、桌面软件、在线工具或教程
- Google Play 中对应关键词的竞品数量、标题用词和评价规模
- 目标页面的下载转化意图

判断优先级时，不以搜索量单项决策，使用：

**机会优先级 = 相关性 × 转化意图 × 竞争可进入性 × 内容可证明程度。**

## 4. 官网 SEO 规划

### 4.1 页面与搜索意图映射

当前站点已有四类真实功能页，不应为了关键词重复生成薄页面。推荐映射如下：

| 页面 | 主要搜索意图 | 推荐主关键词 | 内容重点 |
| --- | --- | --- | --- |
| 首页 | 产品发现 | `photo metadata editor for Android` | EXIF、IPTC、XMP 总能力、Android、下载入口 |
| 元数据编辑页 | 功能评估 | `EXIF and IPTC metadata editor` | 字段范围、处理后副本、单张覆盖、真实截图 |
| 隐私清理页 | 隐私问题解决 | `remove EXIF metadata Android` | GPS、相机信息、选定清理、原文件与副本边界 |
| 批量预设页 | 批量工作流 | `batch IPTC metadata editor` | 预设、批量字段、单张覆盖、输出检查 |
| 照片交付页 | 文件交付 | `photo upload queue Android` | 队列、重试、FTP/FTPS/SFTP/HTTP/SMB/WebDAV |
| 博客指南 | 教程和问题解释 | 具体任务型长尾词 | 真实步骤、限制、FAQ、功能页内链 |

### 4.2 是否创建独立 EXIF/XMP 页面

当前不建议立即新增完全独立的 EXIF 页和 XMP 页，因为现有产品内容主要集中在同一个元数据编辑工作流，拆分后可能造成重复内容和关键词互相竞争。

只有满足以下条件时才拆页：

- 页面有独立的用户任务，而不是只替换标题中的关键词。
- 有对应的真实应用截图或操作步骤。
- 有字段范围、格式限制和输出行为说明。
- 有独立 FAQ、结构化数据和站内链接。
- 能导向明确的 Google Play 下载或功能评估动作。

### 4.3 首页建议文案

英文 title：

**Photo Metadata Editor for Android | CaptionMeta**

英文 description：

**Edit supported EXIF, IPTC, and XMP photo metadata on Android. Clean selected private data, reuse batch presets, and prepare photo copies for delivery with CaptionMeta.**

英文 H1：

**Photo metadata, ready for the field.**

首屏 supporting copy：

**Edit captions, keywords, credits, rights, and supported EXIF, IPTC, and XMP fields on Android. Review the result, keep the original separate, and deliver a processed photo copy when ready.**

中文 title：

**CaptionMeta：Android 照片元数据编辑器**

中文 description：

**在 Android 上编辑支持的 EXIF、IPTC 和 XMP 照片元数据，清理选定的隐私信息，复用批量预设，并准备照片副本进行交付。**

### 4.4 页面质量要求

每个页面至少应具备：

- 唯一的 title、description、H1 和 canonical
- 一个明确的主要搜索意图
- 首屏说明真实产品用途，而不是关键词列表
- 至少一个真实应用截图或视频
- 功能、输入、输出、限制和操作步骤
- 与相关功能页及 Google Play 的上下文链接
- 与页面语言一致的正文、FAQ、图片 alt 和结构化数据
- 不依赖 JavaScript 才能读取的主要内容

FAQ 应面向用户任务。不要公开“项目中包含某测试文件”这类内部实现证据；证据用于内容审核，公开答案应描述用户能做什么以及有什么限制。

## 5. 多语言 SEO

当前站点有 `zh-CN`、`en-US`、`ja-JP`、`ko-KR`、`de-DE`、`fr-FR`、`es-419`、`pt-PT`、`it-IT`、`ru-RU` 和 `ar` 页面。发布前需要逐语言完成以下检查：

- title、description、H1 和 CTA 由母语人员审核
- 关键词按当地用户表达重新研究，不直接翻译英语关键词
- hreflang 双向互链，canonical 指向当前语言页面
- 图片文字和截图与页面语言一致
- Google Play 对应语言的 app name、短描述、完整描述和截图一致
- 不把机器翻译状态标记为正式发布内容

当前发布配置显示大多数语言仍为 `machine-draft`，因此多语言页面应先完成审核，再提交索引和推广。

## 6. Google Play ASO

### 6.1 平台限制

Google Keyword Planner 的 Web 搜索量不能等同于 Google Play 搜索量。标题和描述需要另行验证竞品、排名和转化。

Google Play 文案至少要满足：

- App name 不超过 30 个字符
- Short description 不超过 80 个字符
- Full description 不超过 4,000 个字符
- 关键词自然出现，不能机械重复
- 文案与实际功能、截图和当前版本一致

### 6.2 App name 建议

文档中的 `CaptionMeta: EXIF & IPTC Editor` 为 31 个字符，不能直接使用。

首选测试版本：

**CaptionMeta: EXIF/IPTC Editor**（29 个字符）

备选版本：

- `CaptionMeta - EXIF/IPTC Editor`（30 个字符）
- `CaptionMeta EXIF IPTC Editor`（28 个字符）

正式选择前，应记录每个版本在目标国家的竞品覆盖、搜索展示和商店转化数据。不要仅根据 Web 搜索量改标题。

### 6.3 Short description 建议

英文候选：

**Edit EXIF, IPTC & XMP photo metadata on Android.**

该文案长度为 48 个字符，覆盖核心能力和平台，不宣称完全离线。若产品验证了主要处理流程确实在设备端完成，可在完整描述中解释本地处理边界；不要把服务器交付流程描述成离线功能。

中文候选：

**在 Android 上编辑 EXIF、IPTC、XMP 照片元数据并清理隐私信息。**

### 6.4 Full description 结构

推荐顺序：

1. 一句话说明目标用户和核心任务
2. EXIF、IPTC、XMP 元数据编辑
3. 隐私元数据清理
4. 批量预设与单张覆盖
5. 照片交付队列和支持的协议
6. 文件处理、原文件和处理后副本的边界
7. 适用人群与限制
8. Google Play 下载或更新 CTA

英文开头候选：

**CaptionMeta is a photo metadata editor for Android photographers, journalists, agencies, and field teams. Edit supported EXIF, IPTC, and XMP fields, clean selected private metadata, apply batch presets, and prepare processed photo copies for delivery.**

完整描述中应优先使用真实功能词，而不是重复 `editor`、`metadata` 等关键词。不要承诺云存储、绝对隐私、所有格式支持或未确认的价格权益。

### 6.5 商店素材

首屏前三张截图应依次说明：

1. 编辑 EXIF/IPTC/XMP 字段
2. 批量预设和单张覆盖
3. 隐私清理或照片交付队列

截图必须来自当前版本，文字少而清晰，不能使用未实现的功能示意。当前仓库标记多语言截图仍需人工审核。

## 7. 当前仓库差距

根据当前配置，策略落地前仍有以下工作：

- `seo-geo/keyword-map.csv` 的首页主关键词已调整为 `photo metadata editor`；元数据功能页仍以 IPTC 为专业意图，需要继续验证 EXIF、IPTC、XMP 的页面分工。
- 站点已有四类功能页，但没有独立且内容充分的 EXIF/XMP 页面；先优化现有元数据编辑页。
- `aso/en-US/listing.yaml` 的短描述目前是 `CaptionMeta`，需要替换为价值表达。
- SEO 和 ASO 审计状态仍为 `draft`。
- 英文及其他目标语言仍标记为 `machine-draft`，需要人工审核。
- `editorial.publishedAt` 为空，博客内容不应在未审核时作为正式内容发布。
- 发布配置仍提示 release 签名和 cleartext traffic 需要检查，这些不是 SEO 问题，但会影响最终发布。

## 8. 执行优先级

### P0：发布前必须完成

- 确认生产域名、Google Play 列表和最终包签名
- 修正英文和中文首页 title、description、H1、CTA
- 将 ASO 短描述从产品名改为真实价值表达
- 选择 30 字符以内的候选 App name
- 完成人工语言和截图审核
- 检查 canonical、hreflang、sitemap、robots、Open Graph 和 JSON-LD

### P1：首轮增长优化

- 优化元数据编辑页，使其同时覆盖 EXIF、IPTC、XMP 的真实功能
- 优化隐私清理页，覆盖 GPS 和 EXIF 清理场景
- 完善批量预设和照片交付页的任务型文案
- 为每个功能页补充一个真实 FAQ 和明确下载 CTA
- 建立 Google Search Console 和 Google Play 的基准数据

### P2：验证后扩展

- 根据实际查询词和落地页转化决定是否拆分 EXIF/XMP 页面
- 以英语市场为首轮实验，再扩展其他语言
- 测试 App name、short description、首屏截图顺序和 CTA
- 为已验证的用户问题编写博客指南，避免批量生成重复文章

## 9. 验收指标

### SEO

- 目标页面均有唯一 title、description、H1、canonical 和有效 hreflang
- 重要页面能在无 JavaScript 时读取主要正文和下载 CTA
- Google Search Console 收录状态正常，无明显重复 canonical 或 hreflang 错误
- 按国家和语言记录展示、点击、平均排名和下载转化
- 功能页带来的下载点击率高于纯品牌页面，才继续扩大对应关键词投入

### ASO

- 所有语言的 app name、short description 和 full description 通过字符限制校验
- 首轮实验至少记录曝光、商店访问、安装转化和留存变化
- 截图、文案与当前版本真实功能一致
- 不以 Web 搜索量直接推断 Google Play 排名效果

## 10. 最终原则

CaptionMeta 的正确策略不是放弃 IPTC，而是重新分配它在漏斗中的位置：

**用 Photo Metadata 和 EXIF 获得更广的入口，用 IPTC 和 XMP 建立专业信任，用批量处理、隐私清理和照片交付完成转化。**

所有 SEO 和 ASO 文案必须以已验证的产品功能为边界。关键词服务于用户任务和页面转化，不能反过来要求产品承诺不存在的功能。
